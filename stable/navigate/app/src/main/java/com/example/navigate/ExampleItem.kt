package com.example.navigate

/* A data class to show what a post looks like, it includes an id, a subid, the avatar, the emoji,
* the username and the feelings which is the post */
data class ExampleItem(var uid: String?, var subid: String?, var avatar: Long?, var emoji: Long?,
                       var username: String?, var feelings: String?)
